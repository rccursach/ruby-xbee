#!/usr/bin/env bundle
# encoding: utf-8

source 'http://rubygems.org'

gemspec

group :assets do
  gem 'rake',               '~> 10.1.1'
  gem 'hanna-nouveau',      '~> 0.2.7', :require => false
end

group :test do
  gem 'VersionCheck',       '~> 1.0.0', :require => false
  gem 'simplecov',          '~> 0.8.2', :require => false
  gem 'minitest',           '~> 5.3.0', :require => false
  gem 'minitest-reporters', '>= 1.0.2', :require => false
  gem 'coveralls',                      :require => false
end
