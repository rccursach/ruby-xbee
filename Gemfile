#!/usr/bin/env bundle
# encoding: utf-8

source 'http://rubygems.org'

gemspec

group :assets do
  gem 'rake',               '~> 10.4.2'
  gem 'hanna-nouveau',      '~> 0.4.2', :require => false
end

group :test do
  gem 'VersionCheck',       '~> 1.0.0', :require => false
  gem 'simplecov',          '~> 0.9.1', :require => false
  gem 'minitest',           '~> 5.5.0', :require => false
  gem 'minitest-reporters', '>= 1.0.2', :require => false
  gem 'coveralls',                      :require => false
end
